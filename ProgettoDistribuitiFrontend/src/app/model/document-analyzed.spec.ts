import { DocumentAnalyzed } from './document-analyzed';

describe('DocumentAnalyzed', () => {
  it('should create an instance', () => {
    expect(new DocumentAnalyzed()).toBeTruthy();
  });
});
